import { getOperators } from './operator';
import { getGameTypes } from './gameTypes';
import { getSlateNames } from './slateNames';

export const handlers = [getOperators, getGameTypes, getSlateNames];
